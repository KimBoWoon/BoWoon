<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.rss_reader.RssReaderApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoWoon"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.rss_reader.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.rss_reader.activities.SearchActivity"
            android:exported="false" />
        <activity
            android:name="com.rss_reader.activities.ArticleDetailActivity"
            android:exported="false" />
    </application>

</manifest>